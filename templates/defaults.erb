<% case @def_type
    when 'host'
        symbol = '@'
    when 'user'
        symbol = ':'
    when 'runas'
        symbol = '>'
    else
        symbol = ''
    end -%>
<% if @target then
    symbol = "#{symbol}#{@target}"
end -%>

Defaults<%= symbol %>    <%= Array(@content).join(', ') %>

